#include <stdio.h>

int main()
{
    int a;
    printf("Enter the value of a here: ");
    scanf("%d", &a); // Takes input and stores a
    printf("The value of a is: %d", a);
    return 0;
}