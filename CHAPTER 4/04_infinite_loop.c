// Don't run this program in your system if your system is weak

#include <stdio.h>

int main()
{
    int i = 0;
    while (i < 10)
    {
        printf("The value of i is: %d\n", i);
    }

    return 0;
}